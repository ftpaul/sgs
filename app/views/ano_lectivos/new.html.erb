<%= form_for(@ano_lectivo) do |f| %>
<h2 class="form-signin-heading">Alterar Ano Lectivo</h2>
<div class="alert alert-danger"><b>Atenção!</b> Só alterar esta opção no inicio de cada ano!</div>
<div class="alert alert-warning"><b>Atenção!</b> Colocar no formato AAAA/AAAA!</div>
  <% if @ano_lectivo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ano_lectivo.errors.count, "error") %> prohibited this ano_lectivo from being saved:</h2>

      <ul>
      <% @ano_lectivo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.text_field :ano_lectivo, :class => "form-control", :placeholder => "ano lectivo", :autofocus => true %>

    <%= f.submit 'Alterar Ano Lectivo', :class => 'btn btn-lg btn-primary btn-block' %>
	
    <br>

	  <small><%= link_to 'Cancelar', root_path %></small>

<% end %>



